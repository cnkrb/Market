<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cenkkaraboa.marketynetimuygulamas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarketYönetimUygulaması">
        <activity android:name=".GecmisDetailsActivity"
            android:theme="@style/Theme.MarketYönetimUygulaması.NoActionBar">
        </activity>
        <activity android:name=".UserDetayActivity" />
        <activity
            android:name=".ProductActivity"
            android:theme="@style/Theme.MarketYönetimUygulaması.NoActionBar" />
        <activity android:name=".DetailsActivity" />
        <activity
            android:name=".SubeActivity"
            android:theme="@style/Theme.MarketYönetimUygulaması.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MarketYönetimUygulaması.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>